{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "echo",
			"type": "shell",
			"command": "g++",
			"args": [
				"-g",
				//for single file
				//  
				"${file}",
				//for project with header
				// 			"${fileDirname}\\*.cpp"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}